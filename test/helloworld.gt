proc main()

    int count[5];
    int x;
    float y;
    bool f;
begin
    read count[0];
    x := 1;
    call a(count[0]);

    if 1.5 && 3 then
        write "asdasda\n";
    fi

end

proc a(ref int x)
begin
    
    x := 1;

end
